# 萌芽记录助手小程序发布清单（微信云托管版）

> 更新：2026-08-03（修正为微信云托管 CloudBase Run 路线 + 切回 prod-d6gj3mfkye02c4455（真实部署环境））
> 后端已部署在云托管环境 `prod-d6gj3mfkye02c4455` / 服务 `backend`；
> 小程序走 `wx.cloud.callContainer` 内网调用，免域名 / 免 ICP 备案 / 免 HTTPS / 免服务器域名白名单。
> 账号：萌芽记录助手（账号 ID `100051286939`，APPID `1462714319`，个人主体）。

---

## 一、公众平台后台当前状态（已核实）

| 项目 | 状态 | 说明 |
|------|------|------|
| 小程序名称 | 萌芽记录助手 | |
| AppID | `wxbff15ebad1d7a2f6` | 与 `miniprogram/project.config.json` 一致 ✅ |
| 小程序信息 | 已完成 | |
| 小程序类目 | 已完成 | |
| 微信认证 | 已完成 | ⚠️ 见「三、主体类型提示」 |
| 小程序备案 | 🟡 管局审核中 | 正式发布前必须通过（已在流程中，被动等待即可） |
| 线上版本 | 无 | 尚未发布 |
| 审核版本 | 无 | 未提交审核 |
| 服务器域名 | 未配置 | ✅ 云托管 `callContainer` **不需要**服务器域名，此状态正常 |
| AppSecret / 代码上传密钥 | 未生成 | 仅 CI/CD 或调微信服务端 API 时才需要，本次不强制 |

---

## 二、关键纠正：旧清单的「硬门槛」已不成立

旧文档写「个人主体上线必须 HTTPS域名 + ICP备案 + 服务器域名配置 + 公网服务器」。
**切换云托管 `callContainer` 后，这四条全部免：**
- 免域名 / 免 ICP 网站备案 / 免 HTTPS 证书 / 免服务器域名白名单 / 免公网服务器
- 小程序只在微信内网调后端，不经过任何公网 Web 域名

所以上线真正只卡两件事：
1. **小程序备案通过**（当前管局审核中，等即可）
2. **提交代码审核并过审**

> 注：这里的「备案」指**小程序备案**（微信平台内强制，2023-09 起所有小程序发布前必须完成），
> 不是网站/域名 ICP 备案。后端域名备案因云托管而天然免除。

---

## 三、主体类型提示（重要，需你确认）

文档里多处写「个人主体」，但后台显示「微信认证：已完成」——
微信认证仅**企业 / 组织 / 媒体 / 政府**主体可做，**个人主体不能认证**。
两者矛盾，请确认实际主体：
- 若确为企业 / 组织（如杭州市丁信幼儿园），教育类目通常可正常过审；
- 若真是个人主体却显示已认证，需核对账号。
类目与「幼儿观察记录 / 家园共育」是否匹配，是审核重点，务必确认。

---

## 四、正式发布流程（修正后）

```
1. 等小程序备案通过（管局审核中，无需操作，留意短信 / 公众平台通知）
2. 安装并登录「微信开发者工具」（本机当前未安装）
3. 导入 miniprogram/ 目录，编译确认 USE_CLOUD=true 生效
4. 真机 / 模拟器联调 callContainer（确认后端接口通）
5. 开发者工具 → 上传代码（填版本号 + 备注）
6. 微信公众平台 → 版本管理 → 提交审核
7. 审核通过 + 备案通过 → 发布上线
```

---

## 五、提交审核前必须处理（体检结论）

| 项目 | 状态 | 说明 |
|------|------|------|
| tabbar 图标 | ✅ 齐全 | `assets/tabbar/` 4 张图标均在 |
| 主包体积 | ✅ 216K | 远低于 2MB 上限 |
| 客户端硬编码密钥 | ✅ 无 | 仅登录表单字段，测试账号只出现在文档里 |
| 隐私合规 | ⚠️ 需处理 | 见下 |

### 隐私合规（重点，影响运行 + 审核）

小程序用到 `wx.chooseMedia`（拍照 / 相册，采集幼儿照片）+ 幼儿姓名等个人信息。
微信要求：
1. 在公众平台「设置 → 服务内容声明 / 隐私保护指引」填写《隐私保护指引》；
2. 小程序首次采集前弹隐私授权（`wx.requirePrivacyAuthorize`）；
3. 必要接口在 `app.json` 声明。
未配置会导致 `chooseMedia` 在真机**直接调用失败**（不只是审核问题）。

→ 可让我帮你加隐私授权弹窗 + `app.json` 声明（约 30 行，可一键合入）。

---

## 六、当前真实阻塞点

1. **小程序备案**：管局审核中（被动等待，约 1–20 个工作日）
2. **代码未上传**（需本机装微信开发者工具）
3. **审核未提交**
4. **隐私授权弹窗未加**（建议上线前补）

---

## 七、下一步动作

- 你来做：装微信开发者工具 → 导入 → 上传 → 公众平台提交审核；备案留意通知。
- 我可帮你：① 补隐私授权弹窗代码 ② 随时核对审核 / 备案状态 ③ 备案通过后协助发布前检查。
