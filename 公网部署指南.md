# 萌芽记录助手 · 公网部署与 HTTPS 域名作战手册（从零开始）

> 适用：你目前没有云服务器、没有域名、没有 ICP 备案，想让小程序正式发布上线。
> 范围：从**采购 → 备案 → 部署 → 微信配置 → 提审发布**全流程。
> 部署形态：标准 Docker 整包（nginx + backend + PostgreSQL），作者已在仓库备好配置。

---

## 〇、先搞清楚三件必须分开的事

很多人卡住是因为把下面三件事混为一谈，它们**互相独立、都要完成**：

| 事情 | 是什么 | 由谁管 | 大概耗时 |
|------|--------|--------|----------|
| ① 小程序备案 | 这个小程序本身的资质备案（你后台看到的「管局审核中」）| 微信公众平台提交，管局审 | 1–3 周 |
| ② 域名 ICP 备案 | 你用来当服务器地址的那个域名要在工信部备案 | 云服务商代提交，管局审 | 1–3 周 |
| ③ HTTPS 证书 | 让域名能用 `https://` 访问的 SSL 证书 | 可用免费 Let's Encrypt | 几分钟（自动）|

**微信硬性要求**：小程序的「服务器域名」必须是**已完成 ② 域名备案**且**配置了 ③ HTTPS** 的域名。否则 `wx.request` 会被微信拦截，正式版根本连不上后端。

> 好消息：本项目后端是 `VISION_PROVIDER=offline`，**不调用任何外部 AI 接口**，部署时不需要买 AI 密钥，也不依赖外网 AI 服务，最省心。

---

## 一、采购清单（你要在云厂商官网自己下单）

推荐 **腾讯云**（和微信同生态，备案入口顺、免费 SSL 证书方便）。阿里云也行，流程类似。

### 1.1 云服务器
- 产品：**轻量应用服务器**（比 ECS 便宜、够用）
- 地域：选离你近的（如「上海」）
- 镜像：**Docker 基础镜像**（或装好系统的 Ubuntu 22.04，之后自己装 Docker）
- 配置：**2 核 2G 起步**（小程序量小，2G 够；想稳选 2 核 4G）
- 带宽：峰值 4–6 Mbps 足够（小程序图片不大）
- 系统盘：40G SSD
- 费用：约 **60–120 元/年**（新用户首单常更低）

### 1.2 域名
- 在腾讯云「域名注册」买一个，例如 `your-kindergarten.com` 或 `.cn`
- 后缀建议 `.cn` 或 `.com`，备案友好
- 费用：约 **50–100 元/年**
- ⚠️ 买完立刻做**域名实名认证**（上传身份证），否则不能备案

### 1.3 实名认证（两步）
1. **腾讯云账号实名**：个人实名，上传身份证（几分钟）
2. **域名实名**：在域名控制台提交，1 个工作日内通过

---

## 二、ICP 备案（最关键、最耗时）

> 这一步**只能你本人操作**（要填身份证、人脸核验、邮寄幕布/人脸拍照），我无法代做。

1. 腾讯云控制台 → **备案** → 开始备案
2. 选择「网站备案」→ 填写你刚买的域名 + 刚买的服务器 IP
3. 上传身份证、做人脸核验
4. 腾讯云初审（1–2 天）→ 提交管局 → 管局审核（约 1–3 周）
5. 通过后你会拿到**备案号**（如「沪ICP备2026XXXXXX号」）

⏳ **这段时间你可以并行做**：服务器初始化、本地部署演练、把代码上传到体验版测试（体验版不要求备案域名，只要开发者工具勾了「不校验合法域名」就能用）。

---

## 三、备案通过后：DNS 解析

1. 域名控制台 → 解析 → 添加记录
2. 主机记录：`@` 和 `www` 都加
3. 记录类型：`A`
4. 记录值：你的**云服务器公网 IP**
5. 等待生效（几分钟到几小时）

---

## 四、服务器初始化（在服务器上操作）

用 SSH 登录服务器后执行：

```bash
# 1. 安装 Docker + Compose 插件（Ubuntu 示例）
sudo apt update && sudo apt install -y docker.io docker-compose-plugin
sudo systemctl enable --now docker

# 2. 创建项目目录并拉取代码
mkdir -p /opt/mathsprout && cd /opt/mathsprout
# 把本地 Mathsprout-0 整个目录传上去（用 scp / git clone）
# 例如 git clone <你的仓库地址> .

# 3. 开放防火墙（腾讯云控制台安全组也要放行 80/443）
sudo ufw allow 80,443,22/tcp
```

---

## 五、准备生产配置

### 5.1 后端环境变量

复制 `backend/.env.example` 为 `backend/.env`，并按下面改（生产关键项）：

```ini
DEBUG=false
ENVIRONMENT=production

# 数据库（生产用 PostgreSQL，compose 里已配好，密码自己改强一点）
DATABASE_URL=postgresql+asyncpg://postgres:你的强密码@db:5432/kindergarten_math
DATABASE_URL_SYNC=postgresql://postgres:你的强密码@db:5432/kindergarten_math

# AI：离线模式，无需密钥
VISION_PROVIDER=offline
OFFLINE_RESULTS_DIR=./tests/images/golden

# 安全：必须改成随机值！命令生成：
#   openssl rand -hex 32
SECRET_KEY=在这里粘贴生成的32位十六进制

# CORS：微信小程序不强制，但建议填你的域名
CORS_ORIGINS=["https://你的域名.com"]
```

生成随机密钥：
```bash
openssl rand -hex 32
```

### 5.2 nginx 域名替换

编辑 `nginx.conf`，把两处 `your-domain.com` 改成你的真实域名：
```nginx
server_name 你的域名.com;
```
并把 SSL 证书放到 `./certs/` 目录：
- `fullchain.pem`
- `privkey.pem`

证书获取（服务器上，免费）：
```bash
# 用 acme.sh 申请 Let's Encrypt 证书（腾讯云也有免费证书可直接下载）
# 最简：腾讯云 SSL 控制台申请免费证书 → 下载 Nginx 格式 → 上传到 ./certs/
```

### 5.3 关于 frontend（重要）

标准整包里的 `frontend`（Next.js 网页端）是给**老师在浏览器用**的，**小程序完全用不到它**。
- 如果你也想给老师一个网页后台 → 保留，照常 `docker compose up`。
- 如果只为小程序 → 把 `docker-compose.prod.yml` 里的 `nginx` 和 `frontend` 两个 service 删掉/注释，只留 `backend` + `db`，然后**直接在服务器上用 Caddy 或 nginx 反向代理 backend:8000 并配 HTTPS**。

> 小程序只关心：`https://你的域名.com/api/v1/...` 能通。前端网页端可完全不碰。

---

## 六、启动

```bash
cd /opt/mathsprout
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d

# 健康检查
curl -k https://localhost/api/v1/health
docker compose -f docker-compose.prod.yml ps
```

---

## 七、微信公众平台配置服务器域名

回到后台 **管理 → 开发管理 → 开发设置 → 服务器域名**：

| 类型 | 填什么 |
|------|--------|
| request 合法域名 | `https://你的域名.com` |
| uploadFile 合法域名 | `https://你的域名.com` |
| downloadFile 合法域名 | `https://你的域名.com` |

> 只填到域名根，不要带 `/api`。微信允许该域名下所有路径。
> 点击「保存」即可（个人主体也支持配置，只是正式发布时才强制校验）。

---

## 八、改小程序代码 → 上传 → 提审 → 发布

1. 改 `miniprogram/app.js`：把生产 API 地址改成 `https://你的域名.com/api/v1`
   （现在 `API_BASE_DEV` 是局域网 IP，可新增/改用 `API_BASE_PROD`）
2. 微信开发者工具 → 上传代码（填版本号如 `1.0.0`、备注）
3. 公众平台 → **版本管理** → 刚上传的版本点「提交审核」
4. 审核（1–3 天）→ 通过后「发布」

---

## 九、上线前自检清单

- [ ] 云服务器已购、已装 Docker
- [ ] 域名已购、已实名
- [ ] 域名 ICP 备案已通过（拿到备案号）
- [ ] DNS 已解析到服务器 IP
- [ ] 服务器 nginx 配了真实域名 + 有效 HTTPS 证书
- [ ] `backend/.env` 里 `SECRET_KEY` 已换成随机值、`DATABASE_URL` 密码已改
- [ ] `docker compose up -d` 后 `/api/v1/health` 返回正常
- [ ] 微信后台「服务器域名」已填 `https://你的域名.com`
- [ ] 小程序 `app.js` 的 API 地址已改为生产域名
- [ ] 代码已上传、已提交审核、已发布

---

## 十、常见问题

**Q：备案要钱吗？**  
不要，ICP 备案本身免费。只花域名 + 服务器钱。

**Q：能不能先用临时地址上线？**  
不能。微信正式版强制 HTTPS + 备案域名。开发/体验版可以暂不校验（开发者工具勾选即可），但体验版只能你加的「体验者」能用，不能公开搜到。

**Q：我没有公网服务器，能用内网穿透（cpolar）凑合吗？**  
cpolar 免费版地址每次变、且域名不是你备案的，**不能用于正式发布**。只能临时给同事演示网页端用。小程序正式发布必须用固定备案域名。

**Q：PostgreSQL 和 SQLite 选哪个？**  
数据量小（一个幼儿园）用 SQLite 也行，免维护。想规范用 PostgreSQL（compose 已配）。二选一即可。

**Q：HTTPS 证书过期怎么办？**  
Let's Encrypt 90 天一续，可用 acme.sh 自动续期；腾讯云免费证书 1 年一换，记得提前续。
