fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncmy==0.2.10
greenlet>=3.0.0  # SQLAlchemy 异步引擎运行时必需（离线模式也依赖，缺失会 ImportError 崩溃）
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
pillow==11.0.0
opencv-python-headless==4.10.0.84
numpy==2.2.0
httpx==0.28.1
openai>=1.0.0
anthropic>=0.40.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
sse-starlette==2.1.3
aiofiles==24.1.0
reportlab==4.2.5
openpyxl==3.1.5  # Excel 导出（/dashboard/export）依赖
# 微信云托管对象存储访问（教师拍照 → wx.cloud.uploadFile → 后端按 fileID 读取处理）
cos-python-sdk-v5>=1.9.26

# PDF support (optional — at least one required for PDF uploads)
# pdf2image==1.17.0  # Requires poppler: https://github.com/Belval/pdf2image
# PyMuPDF==1.25.0     # Pure Python, no system deps
